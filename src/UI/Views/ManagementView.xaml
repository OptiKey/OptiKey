<UserControl x:Class="JuliusSweetland.ETTA.UI.Views.ManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/JuliusSweetland.ETTA;component/Resources/Icons/KeySymbols.xaml" />
                <ResourceDictionary>
                    <Style x:Key="TabSymbolStyle" TargetType="{x:Type Path}">
                        <Setter Property="Stretch" Value="Uniform" />
                        <Setter Property="Width" Value="20" />
                        <Setter Property="Height" Value="20" />
                        <Setter Property="Margin" Value="2" />
                        <Setter Property="Fill" Value="Black" />
                        <Setter Property="HorizontalAlignment" Value="Right" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
             
    <Grid>
        <Grid.RowDefinitions>
             <RowDefinition/>
             <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
         
        <TabControl Margin="5 5 5 0">
            <TabItem>
                <TabItem.Header>
                    <DockPanel LastChildFill="False">
                        <TextBlock DockPanel.Dock="Left" Text="Visual" VerticalAlignment="Center" Margin="10,0,10,0" />
                        <Path DockPanel.Dock="Right" Data="{StaticResource ThemeSettingsIcon}" Style="{StaticResource TabSymbolStyle}" />
                    </DockPanel>
                </TabItem.Header>
                <!--ManagementViews/VisualView-->
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <DockPanel LastChildFill="False">
                        <TextBlock DockPanel.Dock="Left" Text="Words" VerticalAlignment="Center" Margin="10,0,10,0" />
                        <Path DockPanel.Dock="Right" Data="{StaticResource WordSettingsIcon}" Style="{StaticResource TabSymbolStyle}" />
                    </DockPanel>
                </TabItem.Header>
                <!--ManagementViews/WordView-->
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <DockPanel LastChildFill="False">
                        <TextBlock DockPanel.Dock="Left" Text="Sounds" VerticalAlignment="Center" Margin="10,0,10,0" />
                        <Path DockPanel.Dock="Right" Data="{StaticResource SoundSettingsIcon}" Style="{StaticResource TabSymbolStyle}" />
                    </DockPanel>
                </TabItem.Header>
                <!--ManagementViews/SoundView-->
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <DockPanel LastChildFill="False">
                        <TextBlock DockPanel.Dock="Left" Text="Pointing &amp; Selecting" VerticalAlignment="Center" Margin="10,0,10,0" />
                        <Path DockPanel.Dock="Right" Data="{StaticResource PointingAndSelectingSettingsIcon}" Style="{StaticResource TabSymbolStyle}" />
                    </DockPanel>
                </TabItem.Header>
                <!--ManagementViews/PointingAndSelectingView-->
            </TabItem>
        </TabControl>
 
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
             <Button Command="{Binding OkCommand}" Width="100" Margin="5 10">OK</Button>
             <Button Command="{Binding CancelCommand}" Width="100" Margin="5 10">Cancel</Button>
        </StackPanel>
    </Grid>
</UserControl>

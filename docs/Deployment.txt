1.Set the AssemblyVersion in AssemblyInfo
2.Commit to GitHub
3.Change app.manifest to include uiAccess="true" flag
4.Build in Release config
5.Sign the exe: signtool sign /v /f "MY_CERTIFICATE.pfx" "OptiKey.exe"
6.Create setup for this version
7.Create release (tag) on GitHub including;
  i)Release notes
  ii)Binary installer
